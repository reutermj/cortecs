component Position(x: Int, y: Int)
component Velocity(x: Int, y: Int)

fn foo(e) {
    let a = {Position(1, 2), e}
    return a
}

fn move() {
    let a = foo(Velocity(3, 4))
    let b = foo({Velocity(3, 4)})
}