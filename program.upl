component Position(x: Int, y: Int)
component Velocity(dx: Int, dy: Int)

fn foo() {
    let a = {Position(1, 2), Velocity(3, 4)}
    let b = a\Velocity
    return b.Position
}

fn move(e) {
    let x = e.Position.x
    let y = e.Position.y
    let p = Position(y, x)
    return {p, e}
}